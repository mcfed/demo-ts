import { AnyAction } from "redux";
import { ICarReducer } from "./Interface";

export default class CarReducer implements ICarReducer {
  private initalState = {};
  saveItem() {
    return "sss";
  }
  getReducer() {}
}
